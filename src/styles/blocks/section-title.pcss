.section-title {
    font-weight: 700;
    font-size: 30px;
    color: $text-color;

    &__subtitle {
        margin-top: 60px;
        display: inline-block;
        font-weight: 600;
        font-size: 16px;
        line-height: 30px;
        color: $gray-text;
    }
}